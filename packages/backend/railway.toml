[build]
builder = "nixpacks"
buildCommand = "npm ci --force"

[deploy]
startCommand = "cd packages/backend && npm run db:migrate && npm start"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
numReplicas = 1