[phases.setup]
nixPkgs = ["nodejs_22", "npm-9_x"]

[phases.install]
cmds = ["cd app && rm -rf node_modules package-lock.json", "cd app && npm install"]

[start]
cmd = "cd app && node src/index.js"

[variables]
NODE_ENV = "production"
CACHE_BUST = "1"